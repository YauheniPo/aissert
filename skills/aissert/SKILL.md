---
name: aissert
description: Evaluate an agent skill against a golden dataset with fact-level LLM verdicts and deterministic Python gates. Use to run full or smoke evaluations and inspect precision, recall, or evaluation artifacts.
---

# aissert

Read `skills/aissert-workflow/SKILL.md` and follow its neutral evaluation
workflow. If the active installation provides an `aissert-*` execution adapter,
use that adapter rather than creating host orchestration yourself. Do not
evaluate or judge yourself; persist each runtime response at the workflow's
artifact path.
